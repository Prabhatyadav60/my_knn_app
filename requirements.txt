# Essential for your KNN model, pin to a stable version
scikit-learn==1.0.2  # A more stable version that works well for deployment

# Cython needed for scikit-learn compilation
Cython==0.29.24

# Flask for the web interface
Flask==2.1.3  # A stable version for the web framework

# Gunicorn for running your Flask app in production
gunicorn==20.1.0

# NumPy, used in many machine learning models
numpy==1.21.6

# pandas for data manipulation (if necessary)
pandas==1.3.5

# If you no longer need Pygame, remove this line
# pygame==2.1.2
